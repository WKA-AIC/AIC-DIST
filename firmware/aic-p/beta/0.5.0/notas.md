# AIC-P 0.5.0

Publicado em 2026-09-12 · canal: beta

Build gerada a partir da branch `legacy-v0.5` (commit `f5cf1d7`) do
repositório AIC-P-FW, com `PROJECT_VER` fixado em `0.5.0` para esta
publicação. Versão legada, mantida para compatibilidade com drivers
AIC-DS na revisão `legacy-v0.5`.

## Atenção
- Mais antiga que a `1.0.0` publicada em `stable` - fica em `beta` por
  decisão explícita, não por ser uma versão de teste mais nova.
